# Changelog

本專案的重要變更紀錄。格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/),版本依 [語意化版本](https://semver.org/lang/zh-TW/)。

## [1.2.0] - 2026-08-17

一次修掉全面檢查找到的問題,重點在「名單會不會外流」與「同時操作會不會出錯」。

### Security
- **場次代碼一律加隨機尾碼**。`/api/list` 沒有權限檢查(投影牆與學員頁都要顯示,加不了密碼),等於知道代碼就看得到名單;而代碼原本是講者自訂的短字串(如 `0613`),試得出來。現在是 `0613-k3f9pm`。
- **匯出 CSV 的權杖改走 `x-admin-token` 標頭**,不再放網址參數(網址會留在瀏覽器歷史與伺服器日誌)。
- **CSV 公式注入**:名字或內容以 `=`、`+`、`-`、`@` 開頭時先中和,避免用 Excel 開名單時被當公式執行。
- **建立場次加上限流**,原本完全沒有,任何人都能無限開場次。
- **投票限流改用來源 IP**。原本用前端送來的 `clientId` 當 key,換一組就繞過,等於沒有限流。
- 新增 `Referrer-Policy: no-referrer`,避免網址裡的場次代碼隨 Referer 外流到 Google Fonts、jsdelivr。

### Fixed
- **同時按讚會掉票**。票數原本是「讀出來→改→寫回」同一筆資料,兩個人同時按就互相覆蓋(投影牆的場景正好是大家同時按)。改成一人一票各存一個 blob。
- **班上有兩個同名的人時,第二個永遠簽不到**,而且看到的訊息是「你已經簽到過了」。現在分成兩種:同一台裝置重複送出才說已簽到,撞名則提示加上可分辨的字。
- **場次被刪除後,投影牆仍顯示「還沒有人加入」並繼續叫人掃 QR**,掃了才發現場次不存在。改為顯示已結束。
- 開場的大 QR 原本每 2 秒重畫一次會閃,改成只畫一次。
- 限流紀錄只增不減,現在由每日排程一併清掉(保留 1 小時)。
- 限流讀寫失敗原本被完全吞掉,失效無聲無息;現在會留 log。
- 學員頁輪詢 2.5 秒與文件寫的 2 秒不一致,統一為 2 秒。

### Changed
- 簽到模式的防重複從「撈出整場逐筆比對」改成「用名字算出 key 查一次」,從 O(n) 變 O(1);人多時不會愈來愈慢。
- `list` / `answer` / `remove` 改用共用的 `lib/http`,不再各自複製一份 `json()`。
- 測試從 13 支增加到 20 支;另有 36 項端到端檢查(對本機 `netlify dev` 實跑)。

## [1.1.0] - 2026-06-19

### Added
- **限流防濫用**:公開的 `checkin` / `vote` 端點加入滑動視窗限流(以 IP / clientId 為 key,上限放寬以相容教室共用 IP),擋腳本式灌爆。
- **資料自動清理**:每日排程函式 `cleanup`,自動刪除建立逾 30 天的場次與其所有資料。
- **隱私說明**:學員頁顯示蒐集與保存說明;新增 `docs/PRIVACY.md`。
- **自動化測試 + CI**:抽出純邏輯到 `netlify/functions/lib/`,以 `node:test` 撰寫單元測試;GitHub Actions 於 push / PR 自動跑測試。
- **LICENSE**(MIT)與本 CHANGELOG。

### Changed
- 後端重構:`session` / `checkin` / `vote` 共用 `lib/`(`validate`、`http`、`ratelimit`),去除重複邏輯。

## [1.0.0] - 2026-06-12

### Added
- 三種模式:**簽到**(實名・防同名・匯出 CSV)、**留言**(可匿名・emoji・❤️)、**Q&A**(可匿名・❤️ 排序・標記已回答)。
- 頁面:封面、講者後台 `/admin`、學員頁 `/s/<代碼>`、投影牆 `/wall/<代碼>`、角落即時 `/ticker/<代碼>`(含 OBS 透明模式)。
- 後台:建立/改名/**刪除**場次、自訂代碼**即時預覽**、連結與 QR、CSV 匯出。
- 加入 QR:學員頁頭像、投影牆(開場大 QR / 進行中右下角)、角落即時(底部常駐)。
- 品牌:講者本人**像素頭像**(全站統一 46px)、Zpix 中文像素字、雜誌 editorial 排版;手機 RWD。
- 文件:README、PRD、SDD。
