# Claude Code 網頁版 — 完整使用手冊

> 給新手（含醫師學員）的一步步教學
> 整理自實際操作問答 ｜ 適用 Claude Code 網頁版 / 手機 App

---

## 📑 目錄

1. [整體畫面介紹](#一整體畫面介紹)
2. [輸入區與設定列](#二輸入區與設定列你打字的地方)
3. [雲端環境設定（Update cloud environment）](#三雲端環境設定update-cloud-environment)
4. [Network access：四種網路權限詳解](#四network-access四種網路權限詳解)
5. [Repo / Branch / GitHub 的關係](#五repo--branch--github-的關係)
6. [Remote Control 遠端控制（多台電腦）](#六remote-control-遠端控制多台電腦)
7. [對話選單（▾）的功能](#七對話選單的功能)
8. [Accept edits 編輯權限模式](#八accept-edits-編輯權限模式)
9. [模型選擇與思考強度](#九模型選擇與思考強度)
10. [語音輸入：讓它聽繁體中文](#十語音輸入讓它聽繁體中文windows)
11. [改動狀態列與 Create PR](#十一改動狀態列與-create-pr)
12. [⚠️ 資料隱私與醫療法規（重要）](#十二️-資料隱私與醫療法規重要)
13. [新手懶人包](#十三新手懶人包)

---

## 一、整體畫面介紹

畫面分成三大區塊：**左邊側邊欄**、**中間歡迎區/設定列**、**下方輸入框**。

### 左邊側邊欄（Sidebar）

| 名稱 | 功能 |
|------|------|
| ➕ **New session** | 開一個全新對話。每次做不相關的新工作就點這個 |
| ⚡ **Routines** | 例行任務，設定固定重複的工作 |
| 🧳 **Dispatch (Beta)** | 把任務派發到背景跑，不用一直盯著 |
| 🧰 **Customize** | 自訂設定（外觀、偏好） |
| **More ⌄** | 展開更多選項（含 Customize sidebar、Archived 封存區） |
| **Recents** | 最近的對話紀錄，點一下回到之前的工作 |
| 左下 **帳號區** | 你的帳號與訂閱方案 |

---

## 二、輸入區與設定列（你打字的地方）

中間「**What's up next?**」是歡迎語。下面那排按鈕是這次工作的設定：

```
[ ☁ Default ]  [ </> my-practice ]  [ ⑂ main ]  [ + ]
```

| 按鈕 | 意思 |
|------|------|
| ☁ **Default** | 選用哪一個**雲端環境**（程式碼在雲端電腦上跑） |
| </> **my-practice** | 選操作哪一個 **GitHub 專案（repo）** |
| ⑂ **main** | 選專案的哪一個**分支** |
| ➕ | 同時加入多個 repo，讓 Claude 一次看好幾個專案 |

### 最底下的輸入框

- **Type / for commands**：打字描述任務，或打 `/` 叫出指令選單
- **Accept edits**：編輯權限模式（見第八節）
- ➕ 附加檔案／圖片
- 🎤 語音輸入
- **Opus 4.8**：目前使用的模型
- **High**：思考強度

---

## 三、雲端環境設定（Update cloud environment）

點 `☁ Default` 旁的設定（齒輪），會看到這個視窗：

| 欄位 | 意思 | 新手要動嗎 |
|------|------|-----------|
| **Name** | 環境名字（預設 Default） | 不用 |
| **Network access** | 網路權限（見第四節） | 保持 Trusted |
| **Environment variables** | 環境變數（給程式用的設定值）| 通常不用 |
| **Setup script** | 開新對話時自動先跑的 bash 指令 | 進階再用 |

### Environment variables（環境變數）

- **誰來寫**：你自己，或讓 Claude 幫你寫
- **格式**：`KEY=VALUE`，一行一個
- **何時需要**：程式需要固定的設定值（如 `NODE_ENV=production`、`PORT=3000`）
- ⚠️ **不要放密碼、API 金鑰！** 任何用這環境的人都看得到

```
NODE_ENV=production
API_BASE_URL=https://api.yoursite.com
GIT_AUTHOR_NAME=Your Name
```

### Setup script（設定腳本）

- **何時執行**：每次開新對話時自動跑，在 Claude 啟動之前
- **何時需要**：每次都要重複初始化時（例如先裝套件）

```bash
#!/bin/bash
npm install          # 每次先裝好套件
cp .env.example .env # 複製環境設定
```

> 💡 一般學習、練習用途，這兩個欄位**完全不用碰**。

---

## 四、Network access：四種網路權限詳解

**重要觀念：「雲端」其實有兩層**

```
你的電腦/手機
     ↕ (控制指令)
Claude 的大腦（Anthropic 伺服器）← 永遠通，不受影響
     ↕ (操作)
你的「雲端工作容器」← Network access 管的是這台對外的連線
     ↕
外部網際網路（npm、GitHub、API…）
```

**Network access 管的是：那台雲端工作容器能不能連到外部網路。**
就算設成 None，Claude 還是能跟你對話、讀改你的程式碼。

| 選項 | 意思 | 何時用 |
|------|------|--------|
| 🔴 **None** | 完全斷網，最安全 | 處理敏感資料，要求不對外傳輸 |
| 🟡 **Trusted**（預設）| 只連可信來源（npm、pip 等套件庫）| **一般用這個就好** |
| 🟢 **Full** | 完全開放，連任何網址 | 需要呼叫外部 API / 資料庫 |
| 🔵 **Custom** | 自訂允許的網域白名單 | 明確知道只需要哪些網站 |

### 醫師情境範例

- **None**：上傳去識別化研究資料做統計分析，要確保不傳到任何外部伺服器
- **Trusted**：學習 Python 分析公開健保資料集，需要裝 pandas、matplotlib
- **Full**：需要即時呼叫醫院 FHIR API 或衛福部開放資料
- **Custom**：只允許連醫院 API 和衛福部資料庫，其他全擋

---

## 五、Repo / Branch / GitHub 的關係

```
GitHub（雲端倉庫，放你所有專案）
   └── Repository（一個專案資料夾，例如 my-practice）
          └── Branch（同專案的不同版本線，例如 main）
```

- **Repo（Repository）** = 一個專案的資料夾，放這個專案所有檔案
- **是不是都在 GitHub？** ✅ 是。網頁版操作的都是存在 GitHub 上的專案
- **分支（Branch）** 是 GitHub 功能，`main` 是主線，做實驗開新分支，不會弄壞 main
- **我的資料需要都放 GitHub 嗎？** ✅ 是。網頁版的流程是：

```
GitHub → 複製到雲端容器 → Claude 改檔案 → 推回 GitHub
```

### Repo 和「Projects」有什麼不同？

| 詞 | 在哪出現 | 意思 |
|----|---------|------|
| **Repository（Repo）** | GitHub / Claude Code | 程式碼專案資料夾，給**開發**用 |
| **Projects** | Claude.ai 聊天介面 | 對話分組，給**組織聊天**用 |

> 比喻：Repo = 放程式碼的工作資料夾；Projects = 你的聊天資料夾。兩個是不同東西。

### 為什麼網頁版要用 GitHub，終端機/桌面版不用？

| 版本 | 程式碼放哪 | 所以… |
|------|-----------|------|
| **終端機 / 桌面版** | 你自己電腦的硬碟 | Claude 直接讀本機資料夾，不需要 GitHub |
| **網頁版 / 手機版** | 雲端的隔離容器（臨時虛擬電腦）| 容器沒有你的檔案，只能從 GitHub 拉取 |

---

## 六、Remote Control 遠端控制（多台電腦）

想用**手機 App 控制桌機**上的程式碼，就用 Remote Control。

- **做法**：在桌機終端機跑 `claude rc`，手機/網頁就能連上那台桌機
- **兩台電腦怎麼辦**：每一台各自跑一次 `claude rc`，那台就會出現在清單裡
- 你選哪一台就控制哪一台，可分開切換

> 💡 記法：**一台電腦 = 跑一次 `claude rc` = 清單多一個選項**

### 對話會共通嗎？

| 版本 | 對話是否共通 |
|------|------------|
| **網頁版（雲端）** | ✅ 集中在帳號，任何電腦打開瀏覽器都看得到 |
| **桌面版** | 存在本機，預設不像網頁版那麼完整同步 |

> 想兩台都看到同一對話 → 用**網頁版**最可靠。

---

## 七、對話選單（▾）的功能

點對話標題旁的 `▾`：

| 選項 | 功能 |
|------|------|
| **Open in → Terminal / Desktop app** | 把對話「搬到」終端機或桌面版繼續做（雲端↔本機切換）|
| **Rename `R`** | 幫對話改名 |
| **Color** | 給對話標顏色（紅/藍/綠…），在側邊欄區分用 |
| **Transcript view** | 看對話記錄的模式（見下） |
| **Copy link `C`** | 複製對話連結 |
| **Edit environment** | 修改這對話用的環境設定 |
| **Archive `A`** | 封存（隱藏但不刪除）|
| **Delete `D`** | 永久刪除 |

### 為什麼要「Open in Terminal / Desktop app」？

網頁版（雲端）不能跑本機程式、不能開瀏覽器看畫面。當你想**在自己電腦繼續跑**、或**看本機畫面**時，就切過去。

### Transcript view：官方建議怎麼選？

| 模式 | 看到什麼 | 何時用 |
|------|---------|--------|
| **Normal** ✅ | 一般對話，只看結果 | **平常用這個**，最乾淨 |
| **Thinking** | 顯示 Claude 思考過程 | 想知道它為什麼這樣判斷 |
| **Verbose** | 所有細節（工具呼叫、系統訊息）| 進階 debug |

### Color 為什麼點了沒反應？

`/color red` 這個**指令在網頁版無效**。要設顏色：點 `▾` → **Color** → 選顏色，會顯示在側邊欄對話列表旁的小圓點。

### Archive 封存：去哪找回來？

- 側邊欄 **More ⌄** 展開 → **Archived** 分類
- **封存 vs 刪除**：封存=隱藏但保留隨時找回；刪除=永久消失
- 用途：完成的對話收起來，側邊欄不雜亂，需要時還能翻出來

### Share session（分享對話）

| 選項 | 意思 |
|------|------|
| **Private**（預設）| 只有你看得到 |
| **Public** | 產生連結，任何人都能看 |

> ⚠️ Public 時不要分享含密碼、金鑰、個資的對話。

### 側邊欄 Recents 對話的 `⋮` 選單

側邊欄每個對話右邊的 `⋮`（管理整個對話用）：

| 選項 | 功能 |
|------|------|
| **Open in →** | 搬到 Terminal / 桌面版繼續做 |
| **Pin `P`** | 📌 把對話釘在最上面，常用的不被擠下去 |
| **Rename `R`** | 改對話名字 |
| **Share** | 分享對話（Private / Public）|
| **Copy link `C`** | 複製對話連結 |
| **Move to group →** | 把對話移到某個分組（像資料夾分類）|
| **Archive `A`** | 封存 |
| **Delete `D`** | 永久刪除 |

> 與對話標題 `▾` 選單的差別：這裡多了 **Pin（釘選）** 和 **Move to group（移到分組）**；少了 Color、Transcript view 那種對話內顯示設定。
> 整理小技巧：把常用對話 **Pin** 起來；對話多了用 **Move to group** 分成「學習」「醫師教材」「實作」等群組。

---

## 八、Accept edits 編輯權限模式

點輸入框旁的權限按鈕，通常三種（由寬鬆到嚴格）：

| 模式 | 行為 | 何時用 |
|------|------|--------|
| **Accept edits（自動接受）** | 改檔案不問你，直接改 | 你信任它、想快一點 |
| **Ask / Normal（每次詢問）** | 改檔案前先問你 | 🌟 **新手建議用這個** |
| **Plan mode（只計畫不動手）** | 只給計畫，完全不改檔案 | 想先聽它怎麼想 |

---

## 九、模型選擇與思考強度

### 模型清單（Models）

| 模型 | 特性 |
|------|------|
| **Fable 5 / Fable 5 (1M)** | 新模型，(1M context)=能一次讀超大量內容 |
| **Opus 4.8** | 最強最聰明，適合難任務 |
| **Sonnet 4.6** | 平衡型，快又夠聰明（日常很好用）|
| **Haiku 4.5** | 最快最輕，簡單任務 |
| **Opus 4.7 / 4.6 (Legacy)** | 舊版，灰字代表舊的，一般不選 |

> 建議：一般用 Opus 4.8 或 Sonnet 4.6；想省額度做簡單事用 Sonnet 或 Haiku。
> **(1M context)** = 記憶容量超大，能一次讀整個大專案。

### 思考強度（Effort）

那個 **Faster ⟷ Smarter** 滑桿就是思考強度，點右下角 `High` 字樣調整。

| 設定 | 特性 | 何時用 |
|------|------|--------|
| **Faster（左）** | 快、省用量 | 簡單問題、小改動、聊天 |
| **High / Smarter（右）** | 想得深、仔細，但慢+耗用量快 | 難程式、規劃架構、複雜 debug |

> ⚠️ High 會比較快用完額度，不用每次都最高。

---

## 十、語音輸入：讓它聽繁體中文（Windows）

問題：**麥克風講話都變英文。** 原因是語音辨識語言不是中文。

### 方法 A：Windows 內建（免裝）

1. 按 `Win + H` 叫出語音輸入
2. 點語音工具列的設定（齒輪），語言改成**中文（繁體）**
3. 先把輸入法切到中文再講話

> 缺點：很多人覺得 Win+H 不好用。

### 方法 B：第三方語音工具（推薦）

**Typeless** — AI 語音打字工具：

- ✅ 支援繁體中文，Windows 可用
- ✅ 支援 100+ 語言，可中英混說自動偵測
- ✅ 系統層級，任何 App 都能用（Gmail、Notion、Claude、瀏覽器）
- ✅ 自動去除「呃、嗯」等贅詞
- ⚡ 約 220 字/分鐘（打字只有 45 字/分鐘）
- 另有免費開源版 **OpenTypeless**

> ⚠️ Claude Code 的麥克風用的是你電腦/瀏覽器的語音辨識，所以要去**系統**把語言設成中文。

---

## 十一、改動狀態列與 Create PR

當 Claude 改完東西，會看到類似：

```
my-practice   claude/dreamy-keller-9jqhfm   +760 −2   [Create PR]
```

| 部分 | 意思 |
|------|------|
| **my-practice** | 操作的專案 |
| **claude/dreamy-keller-...** | Claude 自動開的**新分支**（不是 main）|
| **+760** | 新增 760 行（綠=加）|
| **−2** | 刪除 2 行（紅=減）|
| **Create PR** | 建立 Pull Request（提交合併請求）|

### 流程

```
1. Claude 在新分支改東西（保護你的 main 主程式）
2. 改動：加 760 行、刪 2 行
3. 按 Create PR → 變成合併請求
4. 你審核後決定要不要併進 main
```

### ⚠️ 數字越大越好嗎？「不是」！

- 數字只代表「**改動的數量**」，不代表好壞或品質
- 重點是「**改得對不對**」，不是「改了多少」
- 有時刪掉 100 行讓程式更簡潔是好事；加 1000 行把事情搞複雜是壞事
- 高手追求「用更少程式碼做同樣的事」

| 數字大小 | 你該做什麼 |
|---------|-----------|
| 小（+5 −3）| 快速看一下就好 |
| 大（+760 −2）| ⚠️ 要仔細看，確認每處都是你要的 |

---

## 十二、⚠️ 資料隱私與醫療法規（重要）

### 核心事實：資料還是會傳給 Anthropic

```
醫師的電腦
    ↓ 上傳資料 / 貼文字
Claude 的大腦（Anthropic 伺服器）← ⚠️ 這裡一定看得到
```

**「None」只擋住「工作容器連到其他第三方」，但你貼進對話的內容，Anthropic 伺服器一定看得到。**

| 操作 | None 能保護嗎 |
|------|-------------|
| 防止資料被傳到其他第三方 API | ✅ 可以 |
| 防止 Anthropic 自己看到資料 | ❌ 不行 |

### Claude Code 網頁版 vs 一般 Chat：隱私一樣

兩者都會傳給 Anthropic，**隱私等級沒差別**。Claude Code 的價值是「能跑程式、處理大量資料」，不是更安全。

### 正確流程：先脫敏、再給 Claude

```
原始資料（含真實個資）
    ↓ 人工去掉姓名、身分證、生日、住址…
脫敏後的資料（只剩病症、數值、時間序列）
    ↓ 才貼給 Claude 分析
```

> 讓 Claude 幫你**設計脫敏程式**是 OK 的；但**真實個資本身不要貼進去**。

### 三種合規方案詳解

#### 方案一：Anthropic API + 資料處理協議（DPA）
- 付費用 API + 簽合約（承諾不拿你的資料訓練模型）
- 資料**還是傳到 Anthropic**，只是有合約保障
- 適合：有法務部門的大型醫療機構

#### 方案二：本地 LLM（LM Studio）⭐ 對醫師最實際
- 模型下載到自己電腦，**完全在本機跑**，資料不離開電腦
- **電腦有連網路有關係嗎？** 推論時不需要網路，資料不會出去：

| 情況 | 風險 |
|------|------|
| 下載模型（第一次）| 需連網，但只下載模型本身 |
| 實際使用、處理資料 | **不需網路，資料不出去** |
| 軟體遙測 | 可能有匿名使用統計，但不含資料內容 |

> 最保險：設定好、下載好模型後，處理病人資料前**先關 Wi-Fi**。
> 缺點：本地模型能力比 Claude 差很多。

#### 方案三：AWS / Azure 上的 Claude API
- 在通過資安認證的雲端環境用 Claude，可簽 HIPAA BAA
- 資料留在你租用的雲端環境
- 需要 IT 工程師設定，適合醫院 IT 部門等級

### LM Studio vs Claude Code：解決不同問題

```
LM Studio（本地）：資料從頭到尾不離開電腦 → 零傳輸、最安全
Claude Code（網頁，就算 None）：對話內容傳到 Anthropic → 資料離開電腦
```

> ❌ 不能說「有 Claude Code 就不用 LM Studio」——它們解決不同問題。

| 情境 | 用哪個 |
|------|--------|
| 真實病歷、真實個資 | **LM Studio（本地）** 才安全 |
| 已脫敏資料、學習、公開資料 | Claude Code 沒問題 |
| 需要最聰明的 AI 能力 | Claude Code（本地模型差很多）|
| 斷網環境、法規要求零傳輸 | **只能 LM Studio** |

### 台灣醫師：要做到什麼程度才合規？

台灣主要依**個人資料保護法**，病歷屬**特種個人資料**，保護等級最高。

```
資料類型                          → 應該用哪個
─────────────────────────────────────────────
真實病歷 + 可識別個人              → 只能 LM Studio（本地）
  （姓名/身分證/病歷號任一存在）

假名化資料（理論上能回溯）          → LM Studio 或企業級 API

完全去識別化資料（統計用途）        → Claude Code、一般 Chat 都可

完全公開的研究資料集               → 任何工具都可以
```

**「完全去識別化」至少要移除：** 姓名、身分證/護照號、出生年月日（可留年齡區間）、地址（可留縣市）、電話/Email、病歷號/健保卡號、照片/聲音。

**核心原則：能識別到個人的資料，不能離開你控制的環境。**

| 使用者 | 建議 |
|--------|------|
| 個人研究、學習 | LM Studio 本地，或用完全去識別化資料給 Claude |
| 診所、小型機構 | 建立去識別化 SOP，去識別後才用 Claude；敏感分析用 LM Studio |
| 醫院、大型機構 | IT 評估 AWS Bedrock（Claude）方案，簽合規合約 |

---

## 十三、新手懶人包

### 預設組合

| 項目 | 建議設定 |
|------|---------|
| 環境 | Default（不用動）|
| Network access | Trusted |
| Repo | 選你要做的專案 |
| Branch | 先用 `main`，做實驗才換 |
| Accept edits | **每次詢問（Ask）** |
| 模型 | **Opus 4.8** 或 Sonnet 4.6 |
| Effort | 平常中間，難題拉 High |
| 麥克風 | 先設好中文，或裝 Typeless |

### 最簡單的開始流程

```
1. 確認選好 repo 和 branch
2. 在輸入框用中文描述你想做什麼
3. 按 Enter 送出
4. Claude 去看程式碼、動手做、回報結果
```

---

## 附錄：常見指令與快捷鍵

| 動作 | 方式 |
|------|------|
| 叫出指令選單 | 輸入框打 `/` |
| 看改動對比（Diff）| `Ctrl + ⇧ + D` 或右上 □ 按鈕 |
| 加附件 | `Ctrl + U` 或 ➕ |
| 切換模型 | `Ctrl + I` |
| 對話改名 | 選單 `R` |
| 複製連結 | 選單 `C` |
| 封存 | 選單 `A` |
| 刪除 | 選單 `D` |

---

> 📌 **參考連結**
> - Claude Code 網頁版文件：https://code.claude.com/docs/en/claude-code-on-the-web
> - Remote Control 文件：https://code.claude.com/docs/en/remote-control
> - Typeless：https://www.typeless.com/ ｜ OpenTypeless（免費）：https://www.opentypeless.com/en

*本手冊整理自實際操作問答，供教學參考使用。*
